<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <title>Mis Rutinas</title>
</head>

<body>
    <h1>Rutina de <%= user.username %>
    </h1>
    <p><a href="/perfil">Mi perfil</a> | <a href="/rutinas/editar">Editar rutina</a> | <a href="/logout">Cerrar
            sesión</a></p>

    <% if (rutina) { %>
        <h2>
            <%= rutina.titulo %>
        </h2>
        <ul>
            <% rutina.items.forEach(item=> { %>
                <li><strong>
                        <%= item.dia %>:
                    </strong>
                    <%= item.ejercicio %>
                </li>
                <% }) %>
        </ul>
        <% } else { %>
            <p>Aún no tienes rutina.</p>
            <% } %>
</body>

</html>